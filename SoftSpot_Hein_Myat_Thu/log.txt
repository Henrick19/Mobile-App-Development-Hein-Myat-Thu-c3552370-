17/03/26 - implementing details page (2.5 hrs)
		 - I've already got tapped place's detail via [QueryProperty()]
		 - trying to display noise and crowd level with filled and unfilled circle
			- OK problem solved
			- to display dots based on low, medium, high...i need int to map those enum and to display dots
			- luckly ENUM r actually numbers underneath so i manually assigned them int in Enum
			- and included getters called FilledNoiseDots and FilledCrowdDots in models 
			- and i convert them into dots in converter

18/03/26 - Still working on details page
		 - since i assign int to enums manually, it affacted in homepage so i should probably create a method that change enums to int, rather than assigning manually
			- it worked! +2 to enum so low (0) becomes 2 (2 filled dots), medium (1) becomes 3 (3 filled dots), high (2) becomes 5 (5 filled dots)
		 - detail page DONE! (1 HRS)

	

21/03/26 - Starts working on notification page, there r 5 types of noti
			- place addition noti (this come from setting preference)
			- save / remove from fav noti
			- noti when quiet based on best time (schedule noti)
			- clear all favourites noti
			- need notification model
			- since noti r stored in storage, need some method to get them from storage and save noti to the storage in storage service
			- in noti service, i need method to show noti and to save those new noti to the storage via save method from storge service
			- service done  
			- typo error is annoying..

		- working on view and viewModel
		- trying to figure out how to set different background for read and unread noti 
			- ok i used converters to change bool isread value to color and i put some helper color in app resource
			- and ui bind to IsRead
			- use converter for icon as well
			- use converter for created time as well from datetime to relative
			- there will be a dot for unread msg ..so add property for it in model and ui bind to it using Ellipse.IsVisible
			- on noti bell from home page, there should be numbers count for unread noti
				- add property for it in the model and bind to it
			- all done but my ui is not loading any noti
				- fixed! i forgot to put notiservice in constructors and forgot to call shownoti methods 
			- noti page all done (4.5 hrs)

1/04/26 - moving storage from json to SQLite (1 hr)

2/04/26 - profile page implimantation (1 hr)

3/04/26 - fav page implementation (2 hrs)
		- fav page is not displaying any place, i need to figure out why
			- cause that problem is that the list is bound to the wrong property naem
			- viewModel exposes FavouritePlaces with "u" but in xaml, i bind to "FavoritePlaces" without "u" so it is not working

		- setting page implementation (3 hr)
			- I need to make home screen to display places based on user preference 
				- fixed! it was logical error
			- I changed the switch to dark mode in the settings, but when I restart the app, it became light mode with dark mode switch on
				- what caused that was 
				DarkMode was stored in Preferences and the switch was bound to that, but UserAppTheme was only updated when I toggled the switch in code.
				On a fresh launch nothing applied the saved value, so the app stayed light while the switch still showed dark.

				- so retrieve the saved dark mode value from preferences and apply it to UserAppTheme in the constructor of App.xaml.cs, ensuring that the app theme is set correctly on startup based on the saved preference.

4/04/26 - my got crashed when i tried to run on my android (1hr)
		- after debugging, i found out that the problem is in db storage
		On a fresh app install, the SQLite database is created but tables are not yet initialized. 
		When the app loads, a query is executed before the Place table exists, causing an error and making the app crash.
		- To fix this, I need to ensure that the database tables are created before any queries are executed.

