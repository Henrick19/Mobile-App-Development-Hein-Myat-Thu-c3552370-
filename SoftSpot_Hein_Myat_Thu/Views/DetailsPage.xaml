<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="SoftSpot_Hein_Myat_Thu.Views.DetailsPage"
    Title="Place Details"
    Shell.NavBarIsVisible="False">
    <!-- Testing...just demo layout -->
    <Grid RowDefinitions="Auto,*" Padding="20">
        <Label Text="Place's Detail" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" />
        <Button Text="←"
                FontSize="24"
                BackgroundColor="Transparent"
                TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource Gray100}}"
                Clicked="OnBackClicked"
                HorizontalOptions="Start"
                Padding="0,8,0,12" />
        <VerticalStackLayout Grid.Row="1" VerticalOptions="Center" Spacing="8">
            <Label Text="Detail page of ur tapped place"
                   FontSize="18"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center" />
            <Label Text="{Binding SelectedPlace.Name}"
                   FontSize="16"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
